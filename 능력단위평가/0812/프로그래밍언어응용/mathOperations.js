// 두 수의 합을 반환하는 함수
export function add(a, b) {
  return a + b;
}

// 두 수의 차를 반환하는 함수
export function subtract(a, b) {
  return a - b;
};

// 기본 반환
export default function multiply(a, b) {
  return a * b;
}